# Development database
DATABASE_URL=postgresql://ge:ge@localhost:5432/ge?schema=public

# Test database (used by Jest — separate DB on the same Postgres instance)
TEST_DATABASE_URL=postgresql://ge:ge@localhost:5432/ge_test?schema=public
