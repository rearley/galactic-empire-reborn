# Refuse an install on the wrong Node major rather than warning about it.
# `engines` is advisory by default, which is how this repo ran its whole test
# suite on Node 22 for weeks while every declaration said 24. @see issue #31
engine-strict=true
