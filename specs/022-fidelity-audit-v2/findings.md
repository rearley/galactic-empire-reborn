# 022 Fidelity Audit Findings

**Schema:** Each finding has id, sourceRef, tsModule, severity, disposition, testRef, notes.

Severity: `HIGH` = exploitable / game-breaking, `MEDIUM` = wrong but bounded, `LOW` = cosmetic.
Disposition: `fixed` | `deferred` | `n/a` (intentional TS-only behavior, documented).

ID prefix: `S-` scanners, `A-` AI targeting, `P-` persistence, `C-` combat ranges.

## Scanners & visibility

| id | sourceRef | tsModule | severity | disposition | testRef | notes |
|----|-----------|----------|----------|-------------|---------|-------|

## AI targeting & engagement

| id | sourceRef | tsModule | severity | disposition | testRef | notes |
|----|-----------|----------|----------|-------------|---------|-------|

## Ship state persistence

| id | sourceRef | tsModule | severity | disposition | testRef | notes |
|----|-----------|----------|----------|-------------|---------|-------|
| P-001 | GEMAIN.C:1397 `warhupa` (kill-on-disconnect-during-combat: `if (warsptr->cantexit > 0) killem(warsptr,usrn); warsptr->where = -1`) | `backend/src/gateway/game.gateway.ts:230 handleDisconnect` | HIGH | deferred | — | C kills the ship if a player disconnects while `cantexit > 0` (combat-locked) — the canonical anti-rage-quit mechanic. TS code has the call site explicitly disabled with a TODO citing hot-reload false-kills. Currently a player in combat can disconnect to escape with zero penalty. Fix requires distinguishing prod from dev (e.g. `NODE_ENV` gate, or a debounce on rapid reload) before re-enabling. Deferred. |
| P-002 | GEFUNCS.C:182 `initshp` sets `topspeed = shipclass[...].max_warp`; cargo capacity is `shipclass[...].max_tons` | `backend/src/game/ship/ship-state.mappers.ts:63 prismaShipToState`; `backend/src/gateway/game.gateway.ts:160 reconnect path` | HIGH | fixed | `backend/test/unit/ship-state.mappers.spec.ts` | `prismaShipToState` hard-coded `maxTons: 1000`. The boot-time hydrator in `ShipStateService.onModuleInit` set it correctly from `ShipClass.maxTons`, but the gateway reconnect path (US2, line 160) called the mapper directly without re-applying class-derived caps, silently overriding per-class capacity on every reconnect. Exploit: a light fighter (class maxTons ≈ 200) gains 1000-ton cargo allowance for the rest of its session by reconnecting. Fixed by removing the default from the mapper, adding a ShipClass lookup in the gateway reconnect path, and leaving callers responsible for setting `maxTons`. |
| P-003 | GEMAIN.H WARUSR.options (per `ship-state.types.ts` docstrings: options[0]=SCANNAMES, [1]=SCANHOME, [2]=SCANFULL, [3]=MSG_FILTER) | `backend/src/game/ship/ship-state.service.ts:onModuleInit`; `backend/src/gateway/game.gateway.ts:175 reconnect path`; `backend/src/game/ship/ship-state.mappers.ts:71-72` | MEDIUM | fixed | — (folded into P-002 commit — pure derivation from `row.user.options`, no DB needed for test) | Only `scanNames` (options[0]) and `scanHome` (options[1]) were hydrated. `scanFull` and `msgFilter` were permanently `false` for every session, regardless of the User.options array. Fixed in the same commit as P-002 — both options now read at boot and on reconnect. Renderer impact: `scan lo` did not respect the player's full-detail preference. |
| P-004 | GEFUNCS.C:227 `initshp` sets `energy = 50000L` for new ships | `backend/src/game/onboarding/onboarding.service.ts:118` (uses `ENGYMAX=65000`); `backend/src/game/gateway.ts:511` (respawn-on-death also uses 65000) | LOW | deferred | — | C source spawns ships at 50000 energy (≈77% of ENGYMAX). TS spawns at 65000 = full cap. Trivially exploitable for a free 30% energy top-up if a player can force a respawn. LOW because the marginal advantage is small in practice and respawn-on-death is itself a TS deviation (see P-007). Deferred for design review. |
| P-005 | GEFUNCS.C:196-216 `initshp` random placement: `xcoord = NEUTRAL_X + rndm(.9999)`, retry-on-planet-collision | `backend/src/gateway/game.gateway.ts:511` (respawn `xcoord: 0.5, ycoord: 0.5`); `backend/src/game/onboarding/onboarding.service.ts` | LOW | deferred | — | TS uses a fixed spawn point (0.5, 0.5) in sector (0,0). C randomises within sector (0,0) and retries to avoid landing on a planet. Cosmetic — fixed-point spawn is acceptable for a multiplayer port and may even be desirable for deterministic onboarding tutorials. Deferred. |
| P-006 | GEMAIN.C main loop: ship rows persisted ONLY on disconnect (GEMAIN.C:1428 `gepdb(GEUPDATE)` inside `warhupa`) and on `x` exit (GEMAIN.C:2865) — never per-tick | `backend/src/game/ship/ship-state.service.ts:188 flush()` — subscribed to SHIP_UPDATE (1 s tick) | LOW | n/a | `backend/test/game/ship/ship-state.flush-skips-ephemeral.spec.ts` | Intentional TS divergence: persistent web game requires durable state survives a crash, not just clean disconnect. Per-tick dirty-flush is the chosen pattern. Documented in `CLAUDE.md` State Management section. Note one consequence: every Cybertron AI ship UPDATEs Postgres every second it rotates/moves; if scale becomes a problem, add a 30-s debounce keyed on `dirty`. Deferred as performance follow-up, not a fidelity bug. |
| P-007 | GEFUNCS.C:1087 `killem` does `gepdb(GEDELETE, ...)` + `waruptr->noships--`; the player must `new shi` to re-enter | `backend/src/gateway/game.gateway.ts:509 handleCombatShipDestroyed` (resets `damage=0, energy=65000, xcoord=0.5, ycoord=0.5, ...` in place) | MEDIUM | deferred | — | TS auto-resets the dead ship's row to spawn defaults and lets the player rejoin without buying a new hull. C deletes the ship row; the player has lost a hull and must purchase another. The schema's `@@unique([userid])` on `Ship` (one ship per user) makes the C deletion path incompatible with current TS architecture — re-enabling C-correct behavior requires the multi-ship-per-user redesign first. Deferred until ship-fleet model lands. |
| P-008 | GEMAIN.H WARUSR.noships, WARUSR.topshipno — incremented in `initshp` (GEFUNCS.C:266-267), decremented in `killem` (GEFUNCS.C:1272) | `backend/src/game/commands/handlers/new-ship.handler.ts`; nowhere else | LOW | deferred | — | `noships` and `topshipno` columns exist on User and are never updated by any TS code path. They drift permanently at zero after seeding. Consequence is bounded as long as `@@unique([userid])` (one ship per user) remains in force. Deferred — couples to P-007's multi-ship redesign. |
| P-009 | GEFUNCS.C:175 `tossingegame` sets `warsptr->status = GESTAT_USER`; GEMAIN.C:1436 `warhupa` sets `GESTAT_AVAIL` | `backend/src/game/ship/ship-state.mappers.ts:86 stateToPrismaUpdate` (status explicitly stripped from updates); `backend/src/gateway/game.gateway.ts` (no status transitions on connect/disconnect) | LOW | deferred | — | TS never transitions `Ship.status` between USER (1) / AVAIL (0) / AUTO (2) on player login/logout. New player ships are created with `status: 1` and never change. Cybertrons use `status: 2` (correct). Functional impact appears nil because TS uses `ShipStateService` map presence as the "is active" signal instead of the `status` column — but the column remains in the schema for fidelity. Deferred; document as TS-deliberate in `docs/DECISIONS.md` if confirmed. |
| P-010 | GEMAIN.C:1411 `warhupa` guards on `warsptr->status == GESTAT_USER` — only persists ships currently active | `backend/src/game/ship/ship-state.service.ts:188 flush()` — flushes every dirty in-memory state regardless of status; gateway `handleDisconnect` calls `flushAndUnload` unconditionally | LOW | n/a | — | Tied to P-009. The in-memory map IS the activeness gate in TS, so a `status`-guarded flush is redundant. Acceptable. |
| P-011 | GEFUNCS.C:233-234 `initshp` sets `shieldtype = 1, phasrtype = 1` | `backend/src/game/ship/ship-state.service.ts:51-52` runs a self-heal `if (state.phasrtype === 0) {state.phasrtype = 1; ...}` ONLY on `onModuleInit` (boot); the gateway reconnect path (`game.gateway.ts:162 prismaShipToState`) does NOT re-apply it | MEDIUM | deferred | — | A ship created prior to the self-heal logic landing (or via a code path that bypasses initialization defaults) keeps `phasrtype=0` / `shieldtype=0` forever if the only hydration since then has been the gateway reconnect path rather than a full server restart. Combat math elsewhere may treat type=0 as "no weapon installed". Fix is to move the self-heal into `prismaShipToState` or the gateway reconnect path. Deferred — bounded blast radius (only affects ships from the early-development DB); the proper fix is a one-shot DB migration. |
| P-012 | GEFUNCS.C:147-176 `tossingegame` — entry to game broadcasts `ENTSHP/ENTWAR`, sets `status = GESTAT_USER` | `backend/src/gateway/game.gateway.ts:115 handleConnection` (welcome message + `player.joined` broadcast) | LOW | n/a | — | Equivalent broadcast paths exist; nomenclature differs (Socket.io rooms vs. `outsect/outwar`). Match. |
| P-013 | GEFUNCS.C:978-1006 `damageship` death path: clears `damage`, calls `killem`, sets `status = GESTAT_AVAIL`, `where = -1` | `backend/src/game/ship/ship-state.service.ts:160 removeFromGame`; gateway `handleCombatShipDestroyed` resets row in DB | MEDIUM | deferred | — | Per P-007, the death/respawn shape differs significantly. Additionally, the in-memory map entry is removed (`removeFromGame`) by the combat layer, but the gateway reset of the DB row is fire-and-forget (`void this.prisma.ship.updateMany`). If the player reconnects faster than the async update completes, the gateway will `prismaShipToState` the *un-reset* row, then hit the line-165 self-heal-on-damage-≥100 branch (which resets in memory but never flushes the reset — `dirty` is not set). Race window is short but exploitable. Worth tightening once P-007 redesign is on the table. |
| P-014 | GEFUNCS.C:1087 `killem` post-conditions: drops `waruptr->noships`, deletes Ship row, updates User row | `backend/src/game/gateway.ts:509-513` resets Ship row to defaults; no User update | MEDIUM | deferred | — | TS does not touch the User row on death. C decrements `noships` and writes the User. Consequence: stats columns (`User.kills` etc.) are mutated in the combat layer separately — verify they cover all the C-source mutations once the multi-ship redesign (P-007) goes in. |
| P-015 | C source ships are loaded once at session start (GEFUNCS.C:129 `gepdb(GEGET)` inside `warenter`) and held in memory; never re-loaded | `backend/src/game/ship/ship-state.service.ts:onModuleInit` loads ALL ships on boot; gateway `handleConnection` re-hydrates from DB **only if not already in the map** | LOW | n/a | — | TS holds all known ships in memory continuously. Correct for the persistent-world model. Note: if a row is mutated out-of-band (admin tooling, prisma studio), the in-memory copy will silently override on the next flush. Document as a known operational footgun, not a bug. |
| P-016 | GEMAIN.C:gemidnighta (1084-1335) — operates on Postgres-equivalent (Btrieve) rows directly | `backend/src/game/midnight/midnight.service.ts` — runs inside `prisma.$transaction`, no interaction with in-memory `ShipStateService` map | HIGH | deferred | — | Midnight mutates `User` rows (scores, cash decrement, planet production credits) but the in-memory `ShipState.teamcode` is hydrated at boot from `User.teamcode` and never refreshed. If midnight reconciles team membership (zero-team / orphan handling, lines 121-127 of `midnight.service.ts`) and a player is still connected from the previous day, their in-memory `teamcode` is stale until they disconnect/reconnect. Same issue applies to any future midnight-touches-ship mutation (e.g. economic interest). Fix: emit a `midnight.completed` event and re-hydrate active sessions. Deferred — needs a design decision on whether to force-disconnect at midnight, or refresh selectively. |
| P-017 | GEFUNCS.C:1051-1054 — on session cleanup, `gepdb(GELOOKUPNAME, uid)` + `GEDELETE` cleans up orphan ship rows | `backend/src/gateway/game.gateway.ts:138 handleConnection` — recent fix `618c1dc` added a missing-User guard ONLY on the new-player onboarding branch | MEDIUM | deferred | — | The `618c1dc` fix correctly handles the "valid JWT, no User row, no Ship row" scenario. The complementary scenario — "valid JWT, no User row, Ship row exists (FK should prevent this but consider Cybertron/Droid IDs colliding or a prior admin op that disabled FK enforcement)" — is theoretically blocked by Prisma's FK constraint at the schema level. However, the in-memory map can host orphans loaded by `loadShip()` (e.g. by `CybertronRepository.hydrateAll`) — there is no periodic sweep that reconciles `ShipStateService.findAllShips()` against `prisma.user.findMany()`. Add a startup-only invariant check (`noOrphanShipState` per plan §6f) and a ghost-detector tick. Deferred — addressed by the planned invariant in Task 6. |
| P-018 | GEFUNCS.C — `flush failure` is not really a concept (Btrieve transactions are short and synchronous) | `backend/src/game/ship/ship-state.service.ts:198 flush()` catches per-ship error and logs | LOW | n/a | — | The per-ship try/catch is documented as FR-006 ("isolated faults") — a deliberate TS-only resilience improvement. No fidelity drift. |
| P-019 | None — the C source has no concept of "flush mid-tick" | `backend/src/game/ship/ship-state.service.ts:188 flush()` runs `await prisma.ship.update(...)` sequentially inside a SHIP_UPDATE handler. While the await is pending, game logic on the next tick can mutate the same `state` object — including `state.dirty = true` set BEFORE the prior flush's `state.dirty = false` lands. The line `state.dirty = false` in `flush()` (line 196) can therefore overwrite a fresh dirty flag and silently lose the next-tick mutation from a subsequent flush. | MEDIUM | deferred | — | Race window: small but non-zero. Repro shape: tick T flushes → `await prisma.ship.update` → during the await, tick T+1 mutates the same state and sets `dirty=true` → flush of T returns, sets `dirty=false`, dropping the T+1 mutation. The mutation isn't *lost* (still in memory) but won't be persisted until ship is mutated again. Fix: snapshot `state.dirty=false` BEFORE the await, and re-flag on the next mutation. Deferred — small blast radius, but file an integration test once DB is back up. |
| P-020 | None — Btrieve is single-process | `backend/src/game/ship/ship-state.service.ts:188 flush()` iterates `this.map.values()` with awaits inside the loop | LOW | deferred | — | A ship mutated mid-flush (e.g. during the `await` for ship A, the loop hasn't reached ship B yet, and ship B's mutation happens) is correctly flushed on this same tick. Acceptable. Documented for completeness. |

### Scratch — C-source inventory used for the walk

- **Load on logon:** `GEFUNCS.C:79 warenter` → `GEFUNCS.C:104 findships` → `GEFUNCS.C:129 gepdb(GEGET, ...)` → `GEFUNCS.C:153 tossingegame` (sets `status=GESTAT_USER`).
- **Per-tick mutation:** `GEFUNCS.C:931 update_ship` (energy regen, phaser preload, damage repair, shield charge). NO disk write per tick.
- **Save on logoff:** `GEMAIN.C:1397 warhupa` — `gepdb(GEUPDATE)` + `geudb(GEUPDATE)` then `status = GESTAT_AVAIL`. Combat-locked players (`cantexit > 0`) are killed instead.
- **Save on `x` exit:** `GEMAIN.C:2862 mnu_fightsub` — gated on `cantexit == 0`.
- **Death:** `GEFUNCS.C:1087 killem` — `gepdb(GEDELETE)` + `geudb(GEUPDATE)`; `noships--`.
- **Cleanup:** `GEMAIN.C:1051 gepdb(GELOOKUPNAME, uid)` loop on session destroy.
- **Midnight:** `GEMAIN.C:1067-1335 gemidnighta` — runs against User rows, planet rows, mail rows; does NOT touch active `warsptr` arrays.

### Scratch — TS surface inventory

- `ShipStateService` (`backend/src/game/ship/ship-state.service.ts`) — owner, boot-loads, flushes every 1 s.
- `ship-state.mappers.ts` — Prisma row ↔ in-memory state.
- `ShipTickService` (`backend/src/game/ship/ship-tick.service.ts`) — overspeed, repair, shield charge, auto-shield, auto-repair on every SHIP_UPDATE tick.
- `MaintenanceService` (`backend/src/game/ship/maintenance.service.ts`) — repair queue, auto-repair.
- `CybertronRepository` (`backend/src/game/cybertron/cybertron.repository.ts`) — creates User+Ship rows for AI; calls `shipStateService.loadShip(...)`.
- `OnboardingService` (`backend/src/game/onboarding/onboarding.service.ts`) — Ship.create on first connect.
- `NewShipHandler` (`backend/src/game/commands/handlers/new-ship.handler.ts`) — `new shi` purchase.
- `GameGateway` (`backend/src/gateway/game.gateway.ts`) — handleConnection hydrates on reconnect; handleDisconnect calls `flushAndUnload`; `handleCombatShipDestroyed` resets row on death.
- `MidnightService` (`backend/src/game/midnight/midnight.service.ts`) — does NOT touch Ship; only User/Planet/Mail.

## Combat ranges & weapons

| id | sourceRef | tsModule | severity | disposition | testRef | notes |
|----|-----------|----------|----------|-------------|---------|-------|
| C-001 | GECMDS.C:946-1004 `firep` + GEFUNCS.C:2060-2092 `pdamage` (`disfact = 20000 + phasrtype*4000`; damage rolls off as `(1 - dist/disfact)^pfirdist`; `dam=0` beyond disfact) | `backend/src/game/commands/handlers/phaser.handler.ts:144-200` (added `cdistance*10000 > scanRange` continue); `backend/src/game/combat/combat-math.ts:53 phaserDamage` (formula `(p/100)*maxPhaser/(1+range/100)` — separate from this fix, see C-002) | HIGH | fixed | `backend/test/game/commands/handlers/phaser.range.spec.ts` | **This is the across-the-map shot bug.** TS phaser handler enumerates every ship in the galaxy as a candidate and checks only line-of-fire arc; there is no scan-range or `disfact` gate. The damage formula in TS bears no resemblance to C: `cdistance` returns sectors (≈0-30), TS divides by 100, so a target at 20 sectors away still takes ~91% of max damage. Fixed by gating candidates to `cdistance(ship, candidate)*10000 < scanRange` (mirrors `findshp` scan-range gate used everywhere else) so phasers cannot reach beyond scanner range; full C `pdamage` falloff rewrite filed as C-002. C source has no hard range cap on `firep` (GECMDS.C:937-1004); damage falls off via `pdamage`/`disfact`. The TS gate on scanRange is a pragmatic stopgap pending C-002 rewrite of `phaserDamage`. |
| C-002 | GEFUNCS.C:2060-2092 `pdamage` (`disfact=20000+phasrtype*4000; dd=1-dist/disfact; fd=1-foc/11; dp=dd^pfirdist*fd^2*(phasr/100); dam=pdammax*dp`) plus tonnage divisor `tonfact=1+max_tons/TONFACT` in `firep` | `backend/src/game/combat/combat-math.ts:53 phaserDamage`; `backend/src/game/commands/handlers/phaser.handler.ts:153` | HIGH | deferred | — | TS uses an ad-hoc `(p/100)*maxPhaser/(1+range/100)` with no `pdammax`/`pdmax`, no `pfirdist` exponent (default 1), no focus term (`fd^2`), and no `phasrtype` scaling. Result: linear approximation that is wrong at every distance. Deferred — separate from the bug that triggered the audit; full rewrite of `phaserDamage` to match C `pdamage` + the `(1+phasrtype)/2.5 / tonfact` outer scaling in `firep` requires refactoring `phaserDamage` to accept `phasrtype` and target tonnage, plus introducing `pdammax`/`pfirdist` constants (C defaults 200 / 1). Named blocker: depends on adding `PDAMMAX`, `PFIRDST` constants and threading `phasrtype` + target ton through the call. File as separate fix in a follow-up. |
| C-003 | GECMDS.C:1339-1430 `lockon` — required by `cmd_torp` (line 1188) and `cmd_missl` (line 1302). Computes `fact` from speed and distance (`fact *= (5.0-dist)/tor_fact` for torps, `(5.0-dist)/mis_fact` for missiles); returns 1 only if `fact > 0.7`. | `backend/src/game/commands/handlers/torpedo.handler.ts:97-103`; `backend/src/game/commands/handlers/missile.handler.ts:103-110` (both call `findShip` and immediately allocate a slot; no lock-quality gate) | HIGH | deferred | — | C source requires a lock-acquisition roll before a torpedo or missile is allowed to fire. The roll degrades with distance (`5.0 - dist` linear) and with combined speed for torps. With `tor_fact = mis_fact = 1` (C defaults), at `dist >= 4.3` sectors `fact <= 0.7` and the lock fails. **TS allows lock at any distance up to scanRange (10 sectors), which means players and AI can fire torps from 10 sectors away — across half the galaxy.** Deferred from inline fix: the lock-quality computation is a separable function (`computeLockFact(speed, dist, factor)`) but adding it touches three handler call sites + needs an `lockwarn` analog event for the LOCK1/LOCK3 messages. Named blocker: agree on whether to surface the LOCK3 "lock weak" message or silently no-op. The C-001 range fix already prevents the worst case (target must be in scanrange first), but this gate is the second wall and should land before live combat is shipped. |
| C-004 | GECMDS.C:1722-1781 `cmd_mine` — requires `shipclass.has_mine`, neutral-zone gate, cloak gate, `items[I_MINE] > 0`, `margc == 2` with arg `i ∈ [1,50]` as timer, `usermines` (per-player) live-mine cap, `cantexit = FIRETICKS` on success | `backend/src/game/commands/handlers/mine.handler.ts:35-75` (only checks `items[I_MINE]`; timer hard-coded to 30; no class/neutral/cloak/cap/cantexit) | HIGH | deferred | — | TS mine handler skips six validations and ignores the player-supplied timer. Exploits: (a) cloaked ship can lay invisible mines; (b) mines can be laid in the neutral zone where ships are otherwise immune to them (creates a permanent trap on the planet at (0,0)); (c) no `usermines` cap → player can carpet-bomb a sector with their entire ammo stack; (d) hard-coded 30-tick (~3 min) timer means players cannot lay long-fuse traps. Deferred from inline fix: clean fix needs the timer arg parsed (changes `Command.minArgs` and the parser), the `usermines` global from config (currently absent — defaults to 200 from `numopt(USRMINES,1,200)`), and `shipclass.has_mine` exposed on `ShipClassCacheService` (already exists, just not consumed). Named blocker: needs `USERMINES` constant + `getHasMine()` on `ShipClassCacheService` + Command parser flexibility for the timer arg. |
| C-005 | GEFUNCS.C:2661-2672 `ton_fact` — `damfact / (shipclass[shpclass].damfact / 100.0)`. Per-shipclass scalar (e.g. damfact=100 → no scale; damfact=50 → double damage taken; damfact=200 → half damage taken). | `backend/src/game/combat/combat-math.ts:61 tonFact` — `clamp(tonnage/10000, 0.1, 1.0)` | HIGH | deferred | — | TS `tonFact` is a tons-based linear curve clamped to [0.1, 1.0]; the C function is a per-class damage-vulnerability scalar that has nothing to do with tonnage as a derived quantity (it's the explicit `damfact` field on `shipclass`). Direction of multiplication is also wrong: in C, `damfact = damfact / (damfact_field / 100.0)` means a ship with damfact=200 takes HALF damage (the divisor is 2.0); TS multiplies by `tonFact` so a heavy ship (high tons) takes MORE damage. This is the wrong direction. Used in `rollHullDamage` (every projectile hit) and `processMineSweep`. Deferred — requires schema decision: add `damfact` field to `ShipClass` (currently absent) and migrate ships' existing classes to the C source values. Named blocker: `ShipClass.damfact` schema field + seed-table values from the original C source. |
| C-006 | GECMDS.C:937-941 `firep` + GECMDS.C:1029-1035 `firehp` + GECMDS.C:1159-1163 `torp` — all four player weapon commands call `zaphim(ptr,usrn)` and `return` early if `neutral(&ptr->coord)` is true (firing inside the neutral zone is self-zapping suicide) | `backend/src/game/commands/handlers/phaser.handler.ts` (no neutral check); `backend/src/game/commands/handlers/torpedo.handler.ts` (no neutral check); `backend/src/game/commands/handlers/missile.handler.ts` (no neutral check) | MEDIUM | deferred | — | TS allows weapons to be fired from within the neutral zone at sector (0,0). C source treats this as a hard rule: any weapon fire inside neutral self-zaps the firer (huge damage). Mine handler likewise allows mine-laying in the neutral zone (covered by C-004). Deferred — needs a `neutral()` predicate and `zaphim()` damage routine (search for `zaphim` in C source for damage amount). |
| C-007 | GECMDS.C:923-927 `firep` (no fire if `cloak > 0`, prfmsg PCLOKUP); GECMDS.C:1123-1128 `cmd_torp`; GECMDS.C:1234-1238 `cmd_missl`; GECMDS.C:1741-1745 `cmd_mine` | `backend/src/game/commands/handlers/phaser.handler.ts` (no cloak check); `mine.handler.ts` (no cloak check). `torpedo.handler.ts:82-84` ✓ and `missile.handler.ts` — torpedo has it, missile is missing it as well. | MEDIUM | deferred | — | Cloaked ships cannot fire weapons in C. TS phaser handler and mine handler do not check `ship.cloak`. (Torpedo handler does check; missile handler does NOT check despite the docstring claiming validation 1-6 — re-read it.) Deferred. |
| C-008 | GECMDS.C:930-933 `firep` calls `shielddn(ptr,usrn)` if shields up BEFORE firing (then `shieldup` at the end). Mirrors the C "must drop shields to fire phasors" rule. | `backend/src/game/commands/handlers/phaser.handler.ts` — no shieldstat manipulation around fire | LOW | deferred | — | Cosmetic in TS because phaser still fires regardless of shield state, but the C source momentarily drops the shield (and the firer is vulnerable to incoming hits during the FIRETICKS window). Skipping it means a TS player can phaser-fire from a fully shielded position with zero defensive downside. Deferred — interacts with shield auto-cycle logic. |
| C-009 | GECMDS.C:841-865 `cmd_phas` (hyper branch when `where==1` — i.e. at warp); GECMDS.C:1020-1094 `firehp` is a completely separate function: requires `energy >= HPMINFIR (6000)`, debits `HPFIRAMT (5000)` from energy, sets `hypha=1`, beamwidth `HPBEAMW=5`, range-gated by `ddistance < shipclass.scanrange`, damage formula uses `hpdammax/hpfirdst` (different constants from regular phaser), and only hits ships also at warp (`wptr->where==1`) | `backend/src/game/commands/handlers/phaser.handler.ts:122-125` (treats hyper as: beamWidth=HPBEAMW, dischargePercent=100, same damage formula, same range gate=none); no HPMINFIR check, no HPFIRAMT debit, no `hypha` flag, no "victim must also be at warp" gate | HIGH | deferred | — | Hyperphaser path is broken end-to-end. In C, hyperphaser is a separate ritual: only fires at warp, only HITS targets at warp (it's a warp-to-warp weapon), and costs flux energy. TS treats it as "normal phasor but with a 5° beam". Consequences: (a) a player at warp can hyper-fire and damage planet-bound (non-warp) targets — should not happen in C; (b) no energy cost — free shots; (c) no minimum energy gate. Deferred — needs `hypha` field threading and full re-implementation of the `firehp` branch. Sit with C-001/C-002 phaser rewrite. |
| C-010 | GECMDS.C:999, 1082, 1577, 1662, 1999 — `randamage(wptr,othusn)` is called after every phaser hit, hyperphaser hit, torpedo hit, missile hit, and Cybertron weapon hit | `backend/src/game/combat/combat-math.ts:134 randamage` (function exists, ported in F-001/020); call sites: NONE | HIGH | deferred | — | The function was ported in audit 020/F-001 but **is never invoked from any combat handler or tick service**. Subsystem damage (shield/phaser/weapons/engine) never accrues in TS. F-001 is therefore only half-fixed. Filing as new — distinct from F-001 (which renamed the misnamed function); this is wiring. Deferred: requires hit-resolution path to emit a subsystem-damage event AND mutate ship state (no shield/phasor/weapons/engine damage fields are currently in `ShipState`). Named blocker: add subsystem-damage flags to `ShipState` + emit `COMBAT_SUBSYSTEM_DAMAGED` event + define repair semantics. |
| C-011 | GECMDS.C:1339-1430 `lockon` — emits LOCK1/LOCK3 lockwarn messages (player feedback); also gated by `wptr->cloak < 10` (fully cloaked is unlockable) | `backend/src/game/commands/handlers/torpedo.handler.ts` and `missile.handler.ts` — no cloak gate on the **target** (cloaked targets can still be locked) | MEDIUM | deferred | — | TS does not check `target.cloak` when locking. C explicitly checks `wptr->cloak < 10` — fully-engaged cloak makes a ship unlockable. Bundles with C-003 (lockon rewrite). |
| C-012 | GEFUNCS.C:1432 mine sweep — `if (ddist < MINERANGE && (xsect != 0 \|\| ysect != 0))`: ships in the **neutral zone sector (0,0)** are immune to mines | `backend/src/game/combat/combat-tick.service.ts:326-331` ✓ checks coords in (-0.5, 0.5) | LOW | n/a | `backend/test/game/combat/combat-tick.service.spec.ts` (R-3 neutral-zone) | Verified: TS neutral-zone immunity matches C semantics. No drift. |
| C-013 | GECMDS.C:1432 `cmd_torp` — `lockon` checks `wptr->cloak < 10`; GECMDS.C:949 `firep` loops all ships regardless of team; no friendly-fire team check anywhere in `firep`, `firehp`, `torp`, `missl`, or `laymine` | `backend/src/game/combat/combat-tick.service.ts` and all weapon handlers — no team check | LOW | n/a | — | Friendly fire is allowed in C source (no team filter in any weapon path). TS also allows it. Match. Documented in `phaser.handler.ts` docstring FR-005. |
| C-014 | GEMAIN.H:195 `MINERANGE 10000`; GEMAIN.H:80 `PRELOAD 10`; GEMAIN.H:125-126 `MAXTORPS=MAXMISSL=3`; GEMAIN.H:84 `PHABIAS=2`; GEMAIN.H:86-88 `HPMINFIR=6000 HPFIRAMT=5000 HPBEAMW=5`; GEMAIN.H:138 `FIRETICKS=10` | `backend/src/game/constants.ts` (all pinned) | n/a | n/a | `backend/test/unit/gemain-pins.spec.ts` | Constants pinned. Verified. The non-`GEMAIN.H` globals (`pdammax`, `pfirdist`, `hpdammax`, `hpfirdst`, `tor_fact`, `mis_fact`, `phatowrp`, `torpsped`, `mislsped`, `tdammax`, `mdammax`, `minedammax`, `misengfc`, `usermines`) are config defaults from `numopt(KEY, default, max)` calls in `GEMAIN.C:491-600`. C defaults: `pdammax=1`, `pfirdist=1`, `hpdammax=1`, `hpfirdst=1`, `tor_fact=1`, `mis_fact=1`, `phatowrp=0`, `tdammax=1`, `mdammax=1`, `mnedammax=1`, but those are sentinel "1" placeholders in `numopt` — the production game ships with `.cnf` overrides that the C source itself does not contain. TS constants `TDAMMAX=200`, `MDAMMAX=300`, `MINEDAMMAX=150`, `MISENGFC=10`, `TORPSPED=500`, `MISLSPED=300`, `DECODDS=50`, `JAMTIME=20` are TS-chosen defaults — flag as **document the source of these values** but no fidelity drift since the C source itself defers them to config. |
| C-015 | Verification — audit 020 F-001 (`randamage` subsystem-damage function) | `backend/src/game/combat/combat-math.ts:134` | n/a | partial | — | F-001 added the function but it is **never called**. Re-filed under C-010 as a HIGH disposition: still deferred. The renaming portion of F-001 (`rollHullDamage` for projectile rolls) is verified intact. |
| C-016 | Verification — audit 020 F-002 (phaser reload `phasrtype * PRELOAD`) | `backend/src/game/combat/combat-math.ts:119 phaserReloadAmount`; `combat-tick.service.ts:413` | n/a | verified | `backend/test/unit/interceptor-preload.spec.ts` (existing) | F-002 still applied correctly. `phaserReloadAmount(phasrtype)` returns `phasrtype * PRELOAD` and is consumed by the PHYSICS tick. No drift. |

### Cross-cutting notes (out-of-scope sightings)

- **AI scope (T4):** Cybertron tick (`cybertron-tick.service.ts`) computes `cyb_check_lockon` independently of player-side `lockon()` — the AI may decide to fire at a target outside `lockon`'s `fact > 0.7` quality threshold, since AI weapon dispatch does not route through the same `findShip(scanRange)` gate as player commands. The C-001 phaser-range fix this walk applies to player-fired phasers via the handler; **the AI dispatch may bypass it if Cybertrons construct phaser fire events without going through `PhaserHandlerService`**. T4 should verify Cybertron phaser/torp/missile dispatch enforces the same range + lockon gates added here.
- **Scanner scope (T5):** Several weapons now depend on `ShipClassCacheService.getScanRange(shpclass)` as the de-facto weapon-range cap. If `scanRange` is wildly mis-pinned (e.g. seed values use 1,000,000 instead of 100,000), the C-001 range gate is toothless. T5's scanner audit should pin the per-class `scanrange` values from the original `shipclass[]` table in C source.
- **Persistence scope (T2/done):** No weapon-system persistence concerns surfaced.

### Scratch — C-source weapon inventory used for the walk

| weapon | range constant / formula | damage formula | falloff | energy cost | lock |
|--------|--------------------------|----------------|---------|-------------|------|
| Phasor (normal) | soft: damage→0 at `disfact=20000+phasrtype*4000` (cdistance×10000 scale). NO hard range cap; iterates all ships. | `pdammax * (1-dist/disfact)^pfirdist * (1-foc/11)^2 * (phasr/100)` then scaled by `(1+phasrtype)/2.5 / (1+max_tons/TONFACT)` | quasi-polynomial via `pfirdist` exponent (default 1 → linear) | `phasr -= phasr*percent/100` (charge units; flux drain via PENGUSE per reload tick) | none (instant beam) |
| Phasor (hyper) | hard: `ddistance < shipclass.scanrange`; only victims at warp (`where==1`); also requires firer at warp OR `phasrtype >= phatowrp` | `hpdammax * (1-dist/40000.0)^hpfirdst * phasrtype / (1+max_tons/TONFACT)` (sysop=101) | polynomial via `hpfirdst` | `energy >= HPMINFIR=6000`; `energy -= HPFIRAMT=5000` | none |
| Torpedo | scan range (`lockon` re-uses `scanrange`); lock quality `fact>0.7` where `fact=(1.2-speed/5000)*(5.0-dist)/tor_fact` | `tdammax * rndm(.5)+.5` (or `tdammax * rndm(.5)` against shield) then `ton_fact()` | none (binary hit at distance 0) | none (free shot) | yes — `lockon` gate; flies at `torpsped` per tick |
| Missile | scan range; lock quality `fact>0.7` where `fact=(5.0-dist)/mis_fact` | `mdammax * (energy/50000) * (rndm(.5)+.5)` (modulated by ton_fact); shield variant `mdammax*(energy/50000)*rndm(.1)` | linear with stored charge | `energy -= charge/misengfc` per shot | yes — `lockon`; flies at `mislsped` per tick |
| Mine | hard: `MINERANGE=10000` (cdistance×10000) AND not in neutral sector | `minedammax * (1-dist/MINERANGE)^3 * ton_fact` (cubic falloff) | cubic | none on lay | proximity-triggered when `timer==0` |
