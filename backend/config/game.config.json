{
  "_README": [
    "DEVIATIONS ONLY. Every sysop option defaults to the value the original",
    "shipped in MBMGEMSG.MSG (see SYSOP_OPTIONS.canonDefault in",
    "src/game/config/game-config.ts). List an option here only to run it away",
    "from canon on purpose, and record the reason in docs/DECISIONS.md.",
    "",
    "This file previously restated all 51 options, 44 of them at values that",
    "were never canon - mostly a clamp bound picked because the shipped",
    "defaults were believed unrecoverable. Restating a value you did not",
    "choose is how that drift hid: the file looked authoritative and silently",
    "overrode every correction made to the defaults."
  ],
  "world": {
    "UNIVMAX": 100,
    "_PLANTOCK": "canon 360 minutes; deployed at 120 - see docs/DECISIONS.md",
    "PLANTOCK": 120
  }
}